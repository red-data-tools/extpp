notifications:
  email:
    recipients:
      - red-data-tools@ml.commit-email.info

sudo: required

services:
  - docker

install:
  - docker build -t red-data-tools/extpp .

script:
  - docker run red-data-tools/extpp /bin/sh -c "bundle exec rake"
